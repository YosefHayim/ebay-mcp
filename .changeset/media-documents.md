---
"ebay-mcp": minor
---

Add eight Media API tools for image URLs, listing documents and post-order documents.

Document uploads use the existing local media allowlist and enforce the endpoint-specific formats and size limits. Post-order downloads return embedded PDF MCP resources. Recognize the optional post-order OAuth scope without changing default consent requests. All 283 endpoints in the checked-in OpenAPI coverage report are now covered, with 313 registered tools.
